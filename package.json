{
  "private": true,
  "workspaces": [
    "seed-*"
  ],
  "scripts": {
    "reinstall": "yarn rmNodeModules && yarn rmLock && yarn install",
    "rmNodeModules": "rm -rf node_modules seed**/node_modules",
    "rmLock": "rm -f yarn.lock seed**/yarn.lock"
  }
}
